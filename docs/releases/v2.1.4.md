## v2.1.4 — quieter codex tui goal proxy and disabled-account handling

Patch release that fixes Codex TUI goal proxy noise and ensures disabled or workspace-disabled accounts stay disabled at runtime.

### Fixes
- Codex TUI goal proxy now handles snake-case `thread_id` request bodies and local blocked-goal fallback responses. (#462)
- Workspace-disabled and deactivated accounts stay disabled and visible at runtime instead of being hidden by goal fallback. (#463)
- Model-matrix release validation now skips unsupported current-account capabilities by default and no longer follows terminal JSON failures with false-green echoed prompt tokens. (#462, #463)

### Other
- Serialized Vitest release scripts around the known Windows fake-timer runtime-toast timeout and expanded runtime-proxy and model-capability regression coverage. (#462, #463)
- Validated against build, typecheck, script typecheck, lint, full tests, coverage, model matrix, vendor provenance, repo hygiene, package budget, and whitespace checks.
