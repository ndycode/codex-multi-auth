## Auth

### Bugfixes

- Fixed account support for mutable workspaces after login and preserved workspace bindings across refresh flows. (#117)
- Hardened live probe refresh handling so rotated access, refresh, and ID tokens stay aligned through already-best and switch paths. (#120)

## Quota & Forecast

### Features

- Added `codex auth best` for forecast-driven active-account switching from the CLI. (#120)

## Release Hygiene

### Bugfixes

- Fixed install instructions that could cause npm `EEXIST` collisions with official Codex installs. (#97)

### Improvements

- Expanded `auth best` validation and JSON output coverage for help, malformed flags, null storage, concurrency, and sync-state edge cases. (#120)
