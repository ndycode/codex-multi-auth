## v2.1.1 — repair local governance command routing

Patch release that restores routing for the new local governance commands so they stop falling through to official Codex.

### Fixes
- `codex auth usage`, `account`, `budget`, `bridge`, `integrations`, `models`, and `monitor` now stay in the local multi-auth handler.
- `codex auth bridge token list --json` returns valid JSON when the token store is empty and keeps token hashes out of list output.
- Stabilized retry-suite timers after the routing repair.

### Other
- Validated against focused bridge/routing suites, local-bridge and integration command suites, lint, typecheck, build, and an installed local CLI command matrix.
