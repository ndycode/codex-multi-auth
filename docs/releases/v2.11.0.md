# v2.11.0

A minor release. GPT-6 Astra and the Daybreak cyber models are now first-class,
and four latent defects found while wiring them up are fixed. No user
configuration migration is required.

Landed as [#684](https://github.com/ndycode/codex-multi-auth/pull/684).

## Upgrade notes

No migration is required. Install the package and confirm the version:

```bash
npm i -g codex-multi-auth
codex-multi-auth --version
```

The command should report `2.11.0`.
