## v2.1.3 — repair codex desktop history after wrapper sessions

Fixes a regression where wrapper-launched Codex sessions left the Codex Desktop history index in an inconsistent state, plus app-bind status guidance cleanups.

### Fixes
- Wrapper-launched Codex sessions now repair `session_index.jsonl` after known thread-store write noise, with real `session_meta` evidence required and concurrent repairs serialized through the shadow-home lock. (#460)
- App-bind status warnings now resolve the active status path before printing history-visibility and model-speed guidance. (#460)

### Other
- Documented the explicit Codex Desktop history workaround via `codex-multi-auth rotation unbind-app` or `codex-multi-auth rotation disable`. (#460)
- Clarified that model speed and reasoning control remain owned by Codex config or CLI flags through `model_reasoning_effort`. (#460)
- Validated against focused retry, app-bind, wrapper, full test, build, package budget, and installed-session stress checks.
