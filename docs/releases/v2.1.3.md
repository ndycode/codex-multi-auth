## Bug Fixes
- Fixed app-bind history repair so successful wrapper-launched Codex sessions repair `session_index.jsonl` after known thread-store write noise. (#460)
- Required real `session_meta` evidence before session-index repair and serialized concurrent repair attempts through the existing shadow-home lock. (#460)
- Fixed app-bind status warnings to use the resolved active status path before printing history-visibility and model-speed guidance. (#460)

## Documentation
- Documented the explicit Codex Desktop history workaround via `codex-multi-auth rotation unbind-app` or `codex-multi-auth rotation disable`. (#460)
- Clarified that model speed and reasoning control remain owned by Codex config or CLI flags through `model_reasoning_effort`. (#460)

## Chores
- Validated the merged app-bind repair with focused retry, app-bind, wrapper, full-test, build, package-budget, and installed-session stress checks.

## Changelog

Full Changelog: https://github.com/ndycode/codex-multi-auth/compare/v2.1.2...v2.1.3

- #460 Fix app bind history repair @ndycode
